# 2026-09-12 — Source-only promotion and isolated runtime permission revision

Local implementation, not deployed permissions:

- Added an exact source-only selector for current dev SHA/tree and TEST base,
  before credentials. It also covers manual ordinary dispatch. No selection
  preserves ordinary behavior; selected runs produce no deployable artifact.
- Added an independent `api-runtime` one-policy revision after original recovery
  permissions. It preserves all prior policies/trust and uses private versioned
  package/plan inputs and one exact future retained-route capture key.
- Added required observed API inventory/function metadata reads to API recovery.
- Extended synthetic actual-orchestration checks for the third selection,
  private sealed transport, prior policy preservation and wrong API rejection.
  Original supplemental and production baseline hashes are unchanged.
- No AWS write, customer record, account activation or blog readiness is implied
  by this source change. Exact ledgers, effective-access checks and accepted
  independent TEST operations remain required.

Current procedure: [permission revisions](../docs/thn-test-recovery-permissions.md).
