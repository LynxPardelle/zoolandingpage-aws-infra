# Scoped Hub TEST version discovery

2026-09-13, Central Time.

- Add the native Version create-handler discovery action to the existing Hub
  Lambda statement, preserving fixed functions, policy size, and production.
- Add an independently bound `hub-version` recovery-permission selection that
  modifies one existing policy and cannot adopt the external deployment role.
- Keep Image recovery, Auth corrections and original Config/API paths separate.
- Cover exact policy deltas, bindings, a complete verified/executed SDK workflow,
  negative lifecycle states, and retained ownership/security boundaries.

This is source preparation, not evidence that live IAM or the blog is activated.
