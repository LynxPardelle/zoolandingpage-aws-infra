# Zoolandingpage AWS Infra Codex Memory

This repo owns only the serverless frontend infrastructure for `LynxPardelle/zoolandingpage`.

## Durable Rules

- Keep the app source and artifact packaging in `Z:\GitHub\zoolandingpage`.
- Keep CDK infrastructure here.
- Follow `dev -> test -> main` promotion.
- Do not touch EC2 or Dokploy from this repo.
- Keep Route53 cutover disabled until a live CloudFront audit passes for the app and required drafts.
- The app repo publishes browser assets to `zoolandingpage-public-files`; this repo creates private per-environment buckets for the SSR zip and manifest.
- `api.zoolandingpage.com.mx` remains the existing runtime/API front door unless a separate verified backend migration requires changing it.
- Backend SAM deploy roles must bind GitHub OIDC with exact `aud`, environment-scoped `sub`, and branch `ref`: `test` uses `refs/heads/test`; `production` uses `refs/heads/main`.
- 2026-07-09 13:33:59 -06:00: Public infra repo hardening requires pinned GitHub Actions, no skipped promotion guard on manual deploys, branch/environment protection for deployment branches, secret scanning/push protection when available, and Dependabot coverage for npm plus GitHub Actions.
- 2026-07-09 13:41:54 -06:00: Production serverless frontend must deploy in generated-domain audit mode before cutover. Do not attach custom CloudFront aliases until CNAME conflicts are resolved and `FRONTEND_PRODUCTION_CUSTOM_DOMAIN_NAMES_ENABLED=true` is intentionally set for the cutover pass.
- 2026-07-09 13:46:42 -06:00: Do not manage production SSR log groups through CloudFormation while production uses retained resources; a failed alias deploy left `/aws/lambda/zoolandingpage-production-frontend-ssr` in AWS and future deploys should reuse it instead of deleting logs.
- 2026-07-09 14:58 -06:00: Keep `crearpaginaweb.zoolandingpage.com.mx`, `erosbarajas.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, `robertorodriguezrodriguez.zoolandingpage.com.mx`, and `sitiosweb.zoolandingpage.com.mx` out of frontend CloudFront custom aliases until they pass generated-domain browser QA. The production runtime API resolved them to `zoolandingpage.com.mx` / `not-found` during the 2026-07-09 audit.
- 2026-07-09 15:35 -06:00: Serverless frontend CloudFront distributions must route same-origin backend paths directly to API Gateway: `/auth/session*`, `/auth/admin*`, exact `/auth/runtime-config`, `/features/content-hub/*`, `/features/combo-catalog/*`, and `/api-proxy/*`. These were Traefik routes on EC2/Dokploy; without CloudFront behaviors, browser calls fall through to SSR and protected draft admin/runtime features break. Keep `/auth/runtime-config` exact and on the API proxy; do not add broad `/auth/*` because `/auth/callback` remains app-rendered.
- 2026-07-09 16:33 -06:00: `erosbarajas.com` has an issued us-east-1 ACM certificate and is modeled as a generated-domain production frontend for pre-cutover audit. Keep its traffic DNS on EC2 until custom aliases are attached intentionally and browser QA passes.
- 2026-07-09 17:50 -06:00: Retired aliases `crearpaginaweb.zoolandingpage.com.mx`, `erosbarajas.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, `robertorodriguezrodriguez.zoolandingpage.com.mx`, and `sitiosweb.zoolandingpage.com.mx` should not be reattached to CloudFront or DNS during frontend cutover. Clean their exact Route53 records and any matching CloudFront aliases only through the production OIDC alias-ops workflow with explicit confirmation.
- 2026-07-09 17:58 -06:00: Production alias cleanup/preflight must use the separate `zoolandingpage-production-frontend-alias-ops` OIDC role, not the production deploy role. The alias-ops role is intentionally limited to the Zoolandingpage Route53 hosted zone plus CloudFront distribution listing/conflict checks and exact alias removal.
- 2026-07-09 18:28 -06:00: Alias cleanup completed for the five retired `*.zoolandingpage.com.mx` aliases: Route53 and CloudFront alias-ops reported zero remaining records/aliases, and public DNS returned NXDOMAIN. Production custom alias attach remains blocked: deploying with `FRONTEND_PRODUCTION_CUSTOM_DOMAIN_NAMES_ENABLED=true` rolled back on CloudFront CNAME conflict for `FrontendDistributionZoolandingpageMx`. The production variable was reset to `false` and a stable production deploy completed with no changes. Do not enable Route53 cutover until the conflicting CloudFront alias for the `zoolandingpage-mx` group is identified and released. Pre-cutover browser QA with generated CloudFront hosts requires `?draftDomain=`; Zoolandingpage and Eros rendered cleanly, Zoosite rendered with expected unauthenticated/protected endpoint 400/403/401 responses, and Lynx music rendered but hit CORS on direct `api-proxy/read` from the generated host.
- 2026-07-10 11:00 -06:00: Retired `alecfest-voliii.zoolandingpage.com.mx`, `despacholegalastralex.zoolandingpage.com.mx`, `pamelabetancourt.zoolandingpage.com.mx`, and `pokeapi-demo.zoolandingpage.com.mx` by request. Remove them from production CloudFront aliases and Route53 records; keep `zoolandingpage.com.mx` as the only `zoolandingpage-mx` production frontend alias until a new draft/runtime decision says otherwise.
- 2026-07-10 11:33 -06:00: Production custom aliases are attached successfully with `FRONTEND_PRODUCTION_CUSTOM_DOMAIN_NAMES_ENABLED=true`; Route53 cutover is still disabled and `zoolandingpage.com.mx`, `zoositioweb.com.mx`, and `erosbarajas.com` still resolve publicly to EC2 `32.195.120.158`. The old CloudFront distribution tenant for `zoolandingpage.com.mx` was deleted after verifying DNS still pointed to EC2, which cleared the CNAME conflict for `FrontendDistributionZoolandingpageMx`.
- 2026-07-10 13:16 -06:00: `test.zoolandingpage.com.mx` is now the first public serverless frontend cutover. The old CloudFront distribution tenant `dt_3Bhy5qjEUxpR8ghObjxAxxgPRJz` on EC2-backed distribution `E10Y59XAIPQY6A` was disabled/deleted after Route53 still pointed to EC2, then `ZoolandingTest-Zoolandingpage-test-Frontend` deployed with CloudFront alias/cert and Route53 A/AAAA alias records to `dwjxhi1zggvug.cloudfront.net` (`E27T2MENBSWMWJ`). Public health checks passed for `zoolandingpage.com.mx`, `zoositioweb.com.mx`, and `erosbarajas.com` via `?draftDomain=...`; production DNS remains on EC2.
- 2026-07-10 14:35 -06:00: Pre-production serverless front-door checks using `curl --connect-to` and `--ssl-no-revoke` returned HTTP 200 with document shells for `zoolandingpage.com.mx` on `d2ysmvtb9lup56.cloudfront.net`, `zoositioweb.com.mx` on `d3hyhh4s7i6vwz.cloudfront.net`, `erosbarajas.com` on `ds5rkumijh7kr.cloudfront.net`, and `zoositioweb.com.mx/blog`. Public Route53 A records still point those three apex domains to EC2 `32.195.120.158`; production cutover still requires a separate Route53 enable commit promoted through `dev -> test -> main`.
- 2026-07-10 16:00 -06:00: Full modeled production DNS-cutover preflight returned HTTP 200 with document shells through CloudFront for `zoolandingpage.com.mx`, `zoositioweb.com.mx`, `zoositioweb.com`, `sulandingpage.com.mx`, `sulandingpage.com`, `zoolandingpage.com`, `music.lynxpardelle.com`, `alecfest-voliii.lynxpardelle.com`, `erosbarajas.com`, and `zoositioweb.com.mx/blog`. Route53 before cutover still had A records to EC2 `32.195.120.158` for all nine hostnames.
- 2026-07-10 16:13 -06:00: Production DNS cutover completed through PRs #33, #34, and #35; `Deploy Production` run `29126639459` succeeded and `ZoolandingProduction-Zoolandingpage-production-Frontend` reached `UPDATE_COMPLETE`. Route53 now has A/AAAA aliases to CloudFront for all nine modeled hostnames. Public health passed for the six direct 200 hosts; `zoositioweb.com`, `sulandingpage.com`, and `zoolandingpage.com` return expected 301 redirects to their `.com.mx` canonicals. In-app browser desktop and 390x844 mobile audits showed document shells, H1s, and no console warnings/errors on the audited production routes.
- 2026-07-15 07:35 -06:00: Config Authoring production must deploy the exact Lambda ZIP already verified live in test and pin its S3 `VersionId`; its production OIDC role may read versions only from `zoolanding-config-payloads/*` through `s3:GetObjectVersion`. Do not broaden this grant to test, Data Dropper, the SAM bootstrap bucket, or unversioned deployment paths.
- 2026-07-14 23:52 -06:00: The `zoolanding-config-authoring-production-deploy` role may read only `cloudformation:DescribeStacks` on the exact `zoolanding-config-authoring-test` stack so production can resolve the test `FunctionArn` output and compare live Lambda code hashes. Do not broaden this to other test stacks or add `DescribeStackResource`/`ListChangeSets`; rollback is a protected revert and production infra redeploy.
- 2026-07-15 09:04 -06:00: Runtime Read deploys use three identities per cloud environment: an exact repo/environment/ref GitHub OIDC role, a retained CloudFormation execution role scoped to the existing stack resources, and a retained permissions boundary on the SAM-generated Lambda execution role. The GitHub role must never receive Lambda, API Gateway, or application-data permissions; the CloudFormation role must not create roles or mutate inline/attached policies. Runtime IAM expansion requires a separately reviewed infra change. Keep the exact live function, execution-role, API, SAM bucket, and data-resource identifiers in `runtimeReadDeployment`; a replacement must fail closed until those identifiers are re-read from AWS and deliberately updated. The boundary intentionally retains `s3:ListBucket` only on the environment's config payload bucket because S3 otherwise turns optional-object misses into 403 responses that Runtime Read cannot classify as not-found. There is no dev variant.
